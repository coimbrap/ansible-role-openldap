{% for elem in ou %}
dn: ou={{elem.name}},{{ ldap_basedc }}
ou: {{elem.name}}
objectClass: organizationalUnit

{% for org in elem.orgs | default([]) %}
dn: ou={{org}},ou={{elem.name}},{{ ldap_basedc }}
ou: {{org}}
objectClass: organizationalUnit

{% endfor %}
{% endfor %}
